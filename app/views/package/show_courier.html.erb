<h1><%= @courier.name -%></h1>

<ul>
   <% @courier.packages.each do |c| %>
   <li><%= link_to c.tracking_number, :action => "show", :id => c.id -%></li>
   <% end %>
</ul>

<p><%= link_to "Add new Pacage", {:action => 'new' }%></p>